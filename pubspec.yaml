name: habitt_tracker
description: A new Flutter project.

# Prevent accidental publishing to pub.dev
publish_to: "none"

# Version and build number
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  # Uncomment the lines below if you want to add assets later
  # assets:
  #   - assets/
